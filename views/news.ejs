<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }
        header {
            background-color: #916DB3;
            color: white;
            padding: 10px;
            text-align: center;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            z-index: 100;
        }
        .navbar {
            background-color: #916DB3;
            color: white;
            padding: 10px;
            text-align: center;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            z-index: 100;
            display: flex;
            justify-content: space-between; /* Align the elements to the left and right ends */
            align-items: center; /* Vertically center the elements */
        }
        .navbar h2 {
            font-family: "Times New Roman", serif;
            margin: 0;
        }
        footer {
            background-color: #916DB3;
            color: white;
            padding: 10px;
            text-align: center;
            position: fixed;
            bottom: 0;
            left: 0;
            width: 100%;
            z-index: 100;
        }
        .content {
            padding-top: 100px;
            padding-bottom: 70px;
            margin: 0 auto;
            max-width: 1200px;
        }
        h1 {
            background-color: #FCBAAD;
            color: white;
            padding: 10px;
            margin-top: 0px;
            font-family: "Arial", sans-serif;
            text-align: center;
        }
        h3 {
            font-family: "Arial", sans-serif;
            color: #34421E;
            margin: 0;
            padding-bottom: 5px;
            font-family: "Times New Roman", serif;
        }
        ul {
            list-style-type: none;
            padding: 0;
        }
        .row {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-between;
        }
        .article {
            flex: 0 0 calc(32% - 20px);
            margin: 10px;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
            box-sizing: border-box;
            background-color: #FDE5EC;
        }
        /* Updated styles for categories and dropdown */
        .dropdown-container {
            display: flex;
            justify-content: flex-end;
            margin-top: 0px;
            padding: 0 20px;
        }
        .dropdown {
            position: relative;
            cursor: pointer;
            color: #022c14;
            font-size: 18px;
            font-weight: bold;
            padding-right: 15px; /* Add padding to the right */
        }
        .dropdown::after {
            content: "\25BE"; /* Unicode arrow-down character */
            margin-left: 5px;
        }
        .dropdown-content {
            display: none;
            position: absolute;
            background-color: #916DB3;
            border-radius: 5px;
            padding: 10px;
            top: 30px;
            right: 0;
            min-width: 160px;
            z-index: 1;
            box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.2);
        }
        .dropdown:hover .dropdown-content {
            display: block;
        }
        .dropdown-content a {
            color: #fff;
            text-decoration: none;
            display: block;
            padding: 5px;
            transition: background-color 0.3s ease;
        }
        .dropdown-content a:hover {
            background-color: #7A559A;
        }
        .thumbnail {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
        }
        .thumbnail a {
            display: block;
            text-decoration: none;
            color: inherit;
        }
        .thumbnail img {
            max-width: 100%;
            max-height: 200px;
            margin-bottom: 10px;
        }
    </style>
</head>
<body>
    <header>
        <h2>My News App</h2>
    </header>

    <!-- Updated navbar -->
    <div class="navbar">
        <h2>My News App</h2>
        <div class="dropdown">
            <h2>Categories</h2>
            <div class="dropdown-content">
                <a href="/">Top Headlines</a>
                <a href="/business">Business</a>
                <a href="/sports">Sports</a>
                <a href="/entertainment">Entertainment</a>
                <a href="/technology">Technology</a>
            </div>
        </div>
    </div>

    <div class="content">
        <h1><%= title %></h1>
        <div class="row">
            <% articles.forEach((article, index) => { %>
                <div class="article">
                    <div class="thumbnail">
                        <!-- Wrap thumbnail with anchor tag and set href to the article URL -->
                        <a href="<%= article.url %>">
                            <img src="<%= article.urlToImage %>" alt="<%= article.title %>">
                        </a>
                    </div>
                    <div class="dropdown">
                        <h3><%= article.title %></h3>
                        <div class="dropdown-content">
                            <% if (article.urlToImage) { %>
                                
                            <% } %>
                            <p><%= article.description %></p>
                            <a href="<%= article.url %>">Read more</a>
                        </div>
                    </div>
                </div>
                <% if ((index + 1) % 3 === 0) { %>
                    </div><div class="row">
                <% } %>
            <% }); %>
        </div>
    </div>

    <footer>
        <p>&copy; <%= new Date().getFullYear() %> My News App. All rights reserved.</p>
    </footer>

    <script>
        // Add JavaScript to handle dropdown
        const dropdowns = document.querySelectorAll('.dropdown');
        dropdowns.forEach(dropdown => {
            dropdown.addEventListener('click', () => {
                const dropdownContent = dropdown.querySelector('.dropdown-content');
                dropdownContent.style.display = dropdownContent.style.display === 'block' ? 'none' : 'block';
            });
        });
    </script>
</body>
</html>
